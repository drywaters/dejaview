package partials

import "github.com/drywaters/dejaview/internal/model"

// WatchedStatus is retained for legacy references.
templ WatchedStatus(entry *model.Entry, persons []*model.Person) {
	<div></div>
}
